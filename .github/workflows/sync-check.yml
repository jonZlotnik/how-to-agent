name: sync-check

on:
  push:
  pull_request:

jobs:
  sync-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify generated files match knowledge/
        run: python3 scripts/sync_knowledge.py --check
