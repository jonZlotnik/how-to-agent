title: Rule of Minilanguages
summary: >-
  Some problems are most honest when expressed as a tiny language. A pattern matcher, a build description, a routing table — when the natural way to state the rules is *as rules*, code that pretends to be procedural is fighting the domain. A well-chosen minilanguage compresses a class of problems into something readable, declarative, and extensible.
when_to_use: >-
  Use when a problem domain has repeatable declarative structure (rules, schemas, configurations, transformations). Also use when a config file is growing operators, conditionals, or templating. Also use before writing a third bespoke parser this quarter.
related:
  - rule-of-textuality
  - rule-of-generation
  - rule-of-representation
  - reversible-decisions
  - prototype-to-learn
